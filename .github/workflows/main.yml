name: first workflow 

on:
  push:
     branches:
       - main 

jobs:
  examble-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message 
        run: echo "welcome to our first github action workflow!" 
      
      - name: checkout repository
        uses: action/checkout@v6.1.0
        
      - name: list repository files 
        run: ls -la 
